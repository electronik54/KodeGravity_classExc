1. what is int vs Integer, double vs Double?
   - can we use them? 
   - why use int or double instead of Integer or Double if wrapper class are better?
> Its a wrapper class used for Collection data types